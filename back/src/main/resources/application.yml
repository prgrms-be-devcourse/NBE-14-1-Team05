spring:
  application:
    name: myapp

  datasource:
    url: jdbc:h2:~/local
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
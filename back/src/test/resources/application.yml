spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

  mail:
     host: localhost
     port: 3025
     username: test
     password: test
    show-sql: false

  # 테스트 환경에서 실제 SMTP 연결을 시도하지 않도록 설정 추가
  mail:
    host: localhost
    port: 25
